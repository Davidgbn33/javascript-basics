let php = [(popularité = "très bonne"), (utilisation = "back-end")];

let js = [(popularité = "très bonne"), (utilisation = "front-end")];

let language = php;

switch (language) {
  case php:
    console.log(php);
    break;
  case js:
    console.log(js);
    break;
}
